server:
  port: 8200
spring:
  application:
    name: MALL-WEB
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka
feign:
  hystrix:
    enabled: true
management:
  endpoints:
    web:
      exposure:
        include: "hystrix.stream"